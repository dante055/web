const initialTimeRemaining = 100;

const initialFilp = 0;

const defaultDificulty = 'easy';

const difficultyLevel = {
  easy: 16,
  medium: 24,
  hard: 32,
};

export { initialTimeRemaining, initialFilp, defaultDificulty, difficultyLevel };
